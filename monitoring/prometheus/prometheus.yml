global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "fcg-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["fcg-api:8080"]
        labels:
          application: "FIAP Cloud Games API"
          environment: "production"
